{
  "name": "alova-elysia-demo",
  "private": true,
  "workspaces": [
    "api",
    "admin",
    "web"
  ],
  "scripts": {
    "dev": "concurrently \"bun run --cwd api dev\" \"bun run --cwd web dev\" \"bun run --cwd admin dev\"",
    "build": "bun run --cwd api build && bun run --cwd web build && bun run --cwd admin build",
    "start": "bun run --cwd api start",
    "dev:check": "bun run --cwd api dev:check && bun run --cwd web typecheck && bun run --cwd admin typecheck && bun run lint && bun run format:check",
    "lint": "oxlint .",
    "lint:fix": "oxlint --fix .",
    "format": "oxfmt --write . && prettier --write \"admin/**/*.vue\" \"web/**/*.vue\"",
    "format:check": "oxfmt --check . && prettier --check \"admin/**/*.vue\" \"web/**/*.vue\"",
    "typecheck": "bun run --cwd api typecheck && bun run --cwd web typecheck && bun run --cwd admin typecheck",
    "test": "bun run test:unit && bun run test:integration",
    "test:unit": "bun run --cwd api test:unit",
    "test:integration": "bun run --cwd api test:integration",
    "test:e2e:admin": "bun run --cwd admin test:e2e"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "oxfmt": "^0.45.0",
    "oxlint": "^1.60.0",
    "prettier": "^3.8.2"
  },
  "engines": {
    "bun": "1.3.11"
  }
}
