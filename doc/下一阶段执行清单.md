# Harbor 下一阶段执行清单

> 目标：按 P0 -> P1 -> P2 逐步把项目从“可用模板”提升为“长期可复用基座”。

## P0（必须优先完成）

> 当前先只做 P0。本节按“先后端能力 -> 再前端交互 -> 再自动化与安全收口”推进。

### P0-当前状态（已核对）

- [ ] 角色管理可写闭环（后端 + 前端）
  - [x] 角色新增 / 编辑 / 启用禁用 / 删除 API
  - [x] 角色权限分配（role-permission）
  - [x] 角色菜单分配（role-menu）
  - [x] 后台角色管理页接入完整交互（表格、表单、保存反馈）
- [ ] 用户管理可写闭环（后端 + 前端）
  - [x] 用户新增 / 编辑 / 删除 / 批量删除 API（后端已具备）
  - [x] 后台用户管理页接入新增 / 编辑 / 删除 / 批量删除
  - [x] 页面级操作反馈统一（成功、失败、确认弹窗）
- [ ] 页面级 E2E（admin）
  - [x] 登录 -> 菜单渲染 -> 用户/角色列表加载（已有最小脚本）
  - [x] 关键写操作 smoke（新增/编辑/删除覆盖用户+角色主链路）
  - [x] 接入 CI（`Admin E2E (auth + write smoke)`）
- [ ] 依赖安全低风险治理
  - [ ] 执行 `bun audit` 并分级（critical/high/moderate/low）
  - [ ] 只升级低风险依赖并回归 `dev:check` + `test` + `build`
  - [ ] 暂无法处理项写入文档（风险 + 处理计划）

### P0-执行顺序（从现在开始）

- [x] Step 1：补齐角色可写 API（阻塞后续前端）
  - [x] `POST /api/roles`（新增角色）
  - [x] `PUT /api/roles/:id`（编辑角色）
  - [x] `PATCH /api/roles/:id/status`（启用/禁用）
  - [x] `DELETE /api/roles/:id`（删除角色）
  - [x] `PUT /api/roles/:id/permissions`（角色权限分配）
  - [x] `PUT /api/roles/:id/menus`（角色菜单分配）
- [x] Step 2：补齐 admin 角色管理页交互
  - [x] 新增/编辑弹窗表单
  - [x] 启用/禁用切换与删除确认
  - [x] 权限分配与菜单分配抽屉
  - [x] 操作后刷新 + 统一消息提示
- [x] Step 3：补齐 admin 用户管理页写操作
  - [x] 接入新增/编辑/删除/批量删除按钮与流程
  - [x] 二次确认、失败提示、成功反馈统一
- [x] Step 4：补齐 E2E 与 CI
  - [x] 新增用户/角色写操作 smoke 用例
  - [x] 合并到 CI 工作流并本地复跑通过
- [ ] Step 5：完成依赖安全低风险治理
  - [ ] 执行 `bun audit` 并输出风险分级
  - [x] 只升级 low/moderate 且改动可控依赖（已升级 `vite` / `drizzle-kit` / `esbuild`）
  - [x] 回归 `bun run dev:check && bun run test && bun run build`
  - [x] 在文档记录暂缓项与处理计划

### P0-安全暂缓项记录（2026-04-14）

- [ ] `fast-xml-parser`（critical/high/moderate/low，来源 `cos-js-sdk-v5` 传递依赖）
  - 风险：XML 实体扩展与编码绕过类问题
  - 暂缓原因：上游依赖链限制，短期无法单点安全升级
  - 计划：跟踪 `cos-js-sdk-v5` 上游发布，优先升级到已修复版本
- [ ] `drizzle-orm`（high）
  - 风险：SQL identifier 转义缺陷（GHSA-gpj5-g38j-94v9）
  - 暂缓原因：属于核心 ORM 版本升级，需要专项回归数据库层
  - 计划：在 P1 启动专项升级分支，补回归用例后合并
- [ ] `esbuild`（moderate，`drizzle-kit`/`vite` 传递）
  - 风险：dev server 请求读取风险（开发环境）
  - 当前动作：已升级 `vite` / `drizzle-kit` 并重建锁文件
  - 计划：继续跟踪上游彻底修复版本并二次升级

## P1（强烈建议尽快完成）

- [x] 错误码与前端错误映射统一
  - [x] 增加业务错误码字典文档
  - [x] 前端全局错误映射（页面不再各自分散处理）
- [x] 审计日志增强
  - [x] 补齐 operatorUserId 等关键字段
  - [x] 完善检索维度（时间、操作者、成功/失败、模块）
- [x] 就绪检查增强
  - [x] `/ready` 按需增加 Redis 检查
  - [x] 明确失败语义与返回结构
- [x] Admin 构建体积优化
  - [x] 路由/页面懒加载
  - [x] manualChunks 或等效分包策略

## P2（体验与工程化）

- [x] CRUD 基建组件化
  - [x] SearchBar / TableToolbar / FormDrawer 抽象
  - [x] 常见页面模板化（列表 + 搜索 + 表单）
- [x] 全局状态体验统一
  - [x] loading / empty / error 统一组件
  - [x] 页面状态展示风格统一
- [x] 文档完善
  - [x] 发布流程与回滚策略
  - [x] 故障排查（数据库、OpenAPI、权限、CI）
- [x] 安全策略补充
  - [x] CORS 白名单策略细化
  - [x] 限流策略按接口分级

## 每项完成标准（Definition of Done）

- [x] 代码通过：`bun run dev:check`
- [x] 测试通过：`bun run test`（涉及前端交互时补 E2E）
- [x] 构建通过：`bun run build`
- [x] 文档更新：README / CONTRIBUTING / 对应模块文档
- [ ] 提交信息清晰，说明“为什么改”
