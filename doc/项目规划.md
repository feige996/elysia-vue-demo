生成 Vue3+Elysia+Bun+AlovaJS 全栈项目模板的AI Prompt

请作为全栈开发模板生成专家，基于以下明确要求，生成一个可直接落地、结构规范、贴合实操的 `Vue3 + Elysia + Bun + AlovaJS` 全栈项目模板，模板需包含完整目录结构、核心配置文件、关键业务代码（接口、请求、中间件、服务等），无需多余冗余内容，确保开箱即用，适配个人项目、小团队项目及中型后台系统，具体要求如下：

一、核心技术栈（必须严格遵循）

- 前端：Vue3（Composition API + <script setup>）、Vite、TypeScript、AlovaJS（请求+状态管理）、Zod（参数校验）、CSS预处理（可选，如Scoped CSS）

- 后端：Elysia（轻量后端框架）、Bun（运行时，替代Node.js）、TypeScript、Elysia-di（依赖注入）、Zod（接口校验）

- 开发/部署：Bun（依赖管理、热重启）、Vite（前端热重载）、PM2（生产环境守护进程，可选）、Nginx（反向代理，可选）

二、项目目录结构（必须严格遵循，层级清晰，可直接复用）

项目采用前后端分离结构，根目录区分前端（web）和后端（api），各自目录按模块化、分层架构设计，具体结构如下：

# 根目录（项目名称：Harbor / harbor）

harbor/（仓库根目录，npm 包名 `harbor`）
├── api/ # 后端项目（Elysia + Bun + TypeScript + elysia-di + Zod）
│ ├── src/
│ │ ├── index.ts # 后端入口文件（注册中间件、模块、DI依赖）
│ │ ├── middleware/ # 全局中间件目录
│ │ │ └── index.ts # 集中导出日志、权限、错误处理中间件
│ │ ├── modules/ # 业务模块目录（按业务拆分）
│ │ │ ├── user.ts # 用户模块（路由+接口逻辑）
│ │ │ └── article.ts # 文章模块（路由+接口逻辑）
│ │ ├── services/ # 业务服务目录（DI注入，封装业务逻辑）
│ │ │ ├── user.service.ts # 用户服务
│ │ │ └── log.service.ts # 日志服务（示例）
│ │ └── repositories/ # 数据访问层目录（可选，中型项目必备）
│ │ └── user.repository.ts # 用户数据访问（模拟数据库操作）
│ ├── package.json # 后端依赖及脚本配置（Bun相关）
│ └── tsconfig.json # 后端TS配置（极简配置）
├── web/ # 前端项目（Vue3 + Vite + TypeScript + AlovaJS + AlovaJS + Zod）
│ ├── src/
│ │ ├── main.ts # 前端入口文件（挂载Vue实例）
│ │ ├── api/ # 接口请求目录
│ │ │ └── request.ts # Alova实例封装（关联后端类型、全局拦截器）
│ │ ├── views/ # 页面视图目录
│ │ │ ├── UserList.vue # 用户列表页面（示例）
│ │ │ └── Login.vue # 登录页面（示例）
│ │ ├── components/ # 公共组件目录（可选）
│ │ ├── utils/ # 工具函数目录（可选）
│ │ ├── style/ # 全局样式目录（可选）
│ │ └── env.d.ts # 环境变量声明
│ ├── index.html # 前端入口HTML
│ ├── package.json # 前端依赖及脚本配置
│ ├── tsconfig.json # 前端TS配置
│ └── vite.config.ts # Vite配置（极简配置）
└── package.json # 根目录脚本（一键启动/打包前后端）

三、模板生成核心要求（必须全部满足）

1. 完整性：包含上述目录结构中所有文件，每个文件需有可运行的核心代码，无空文件；核心功能（接口、请求、中间件、DI注入、前后端类型同步）必须实现，可直接启动调试。

2. 规范性：代码风格统一（TypeScript规范、Vue3规范），命名清晰（文件、变量、函数命名符合全栈开发习惯），注释简洁明了（关键逻辑、配置项需加注释）。

3. 实操性：

- 后端：实现全局中间件（日志、权限、错误处理）、2个核心业务模块（用户、文章）、DI依赖注入（服务层+可选数据访问层）、接口校验（Zod），导出AppType类型供前端使用。

- 前端：封装Alova实例（关联后端AppType，实现前后端类型同步）、2个示例页面（用户列表、登录）、请求与状态管理（AlovaJS），实现表单提交、列表查询等基础功能。

- 脚本配置：完善根目录、前端、后端的package.json脚本（开发、打包、启动），支持一键启动前后端、热重载。

4. 简洁性：避免冗余代码，不添加无关功能，核心代码聚焦“基础模板”，方便开发者在此基础上快速扩展业务。

5. 兼容性：确保代码可正常运行，Bun、Vue3、Elysia、AlovaJS版本适配（选用最新稳定版），注释中说明关键依赖的安装命令。

四、生成输出要求

1. 先输出完整的项目目录结构（与上述一致），标注每个文件的核心作用。

2. 按目录顺序，依次输出每个文件的完整代码，代码可直接复制使用，无需修改。

3. 最后补充项目初始化、启动、打包、部署的步骤（简洁明了，贴合Bun运行时特性）。

4. 不添加多余的理论说明，聚焦“模板代码+实操步骤”，确保开发者拿到后可直接上手。

请严格按照以上要求生成模板，确保技术栈、目录结构、核心功能完全匹配，贴合前端主导的全栈开发场景，实现“快、轻、闭环、不折腾”的开发体验，模板需开箱即用，无需额外配置。
